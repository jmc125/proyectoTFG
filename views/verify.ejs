<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Verificar Certificado</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <h1>Verificar Certificado</h1>
    </header>

    <main>
        <section>
            <form action="/verify" method="POST">
                <input type="text" name="certificateId" placeholder="ID del Certificado" required>
                <button type="submit">Verificar</button>
            </form>
        </section>

        <% if (searchCompleted) { %>
            <section>
                <% if (result) { %>
                    <h2>Certificado encontrado</h2>
                    <ul>
                        <li><strong>Estudiante:</strong> <%= result.data.studentName %></li>
                        <li><strong>Curso:</strong> <%= result.data.courseName %></li>
                        <li><strong>Hash del Bloque:</strong> <%= result.hash %></li>
                        <li><strong>Validador:</strong> <%= result.validator %></li>
                        <li><strong>Clave Pública:</strong>
                            <pre><%= result.data.publicKey %></pre>
                        </li>
                    </ul>
                    <% if (validSignature) { %>
                        <p style="color:green;"><strong>✔ Firma digital válida (RSA verificada)</strong></p>
                    <% } else { %>
                        <p style="color:red;"><strong>✘ Firma digital inválida o corrupta</strong></p>
                    <% } %>
                <% } else { %>
                    <h2 style="color: red;">Certificado no encontrado</h2>
                <% } %>
            </section>
        <% } %>

        <nav>
            <a href="/">Volver al inicio</a>
        </nav>
    </main>
</body>
</html>
